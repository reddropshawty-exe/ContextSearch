version: "3.9"

services:
  api:
    build: .
    command: uvicorn ui.api.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
  web:
    build: .
    command: streamlit run ui/web/app.py --server.address 0.0.0.0 --server.port 8501
    ports:
      - "8501:8501"
